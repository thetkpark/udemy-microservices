apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  # local:
  #   push: false
  #   useBuildkit: true
  googleCloudBuild:
    projectId: macbigduck
  artifacts:
    - image: asia.gcr.io/macbigduck/thetkpark-ticketing-auth
      context: auth
      docker: 
        dockerfile: Dockerfile
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .
  